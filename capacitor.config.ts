import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'platzi.test.com',
  appName: 'Platzi Music Test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
